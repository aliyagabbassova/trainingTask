server:
  port: 8082

spring:
  application:
    name: company-service
  config:
    name: application-company-service
    import: optional:configserver:http://config-server:8080
  cloud:
    config:
#      fail-fast: false
  datasource:
    url: jdbc:postgresql://postgres-company:5432/company_db
    username: aliya
    password: aliya123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
