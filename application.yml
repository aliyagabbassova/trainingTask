spring:
  application:
    name: company-service
  datasource:
    url: jdbc:postgresql://localhost:5432/users
    username: aliya
    password: aliya123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
    active: dev